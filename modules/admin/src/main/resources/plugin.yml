name: ${project.artifactId}
main : au.com.addstar.geSuitAdmin.geSuitAdmin
version: ${plugin.version}
author: Narimm
description: ${project.description}
commands:
  gesuitdebug:
    usage: /<command>
    description: Turn on debugging.
    permission: geSuit.admin